<Window x:Class="ShinCapture.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="신캡쳐" Width="360" Height="480"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Visibility="Hidden"
        Background="{DynamicResource BackgroundPrimaryBrush}"
        FontFamily="{DynamicResource AppFont}"
        FontSize="{DynamicResource FontSizeNormal}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="캡쳐 모드"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   FontSize="{DynamicResource FontSizeSmall}" Margin="0,0,0,10"/>
        <ItemsControl Grid.Row="1" x:Name="CaptureModesPanel">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="2"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
        <TextBlock Grid.Row="2" Text="최근 캡쳐"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   FontSize="{DynamicResource FontSizeSmall}" Margin="0,16,0,8"/>
        <DockPanel Grid.Row="3" Margin="0,8,0,0">
            <TextBlock x:Name="VersionLabel" Foreground="{DynamicResource TextDisabledBrush}"
                       FontSize="{DynamicResource FontSizeSmall}" VerticalAlignment="Center"/>
            <Button Content="⚙ 설정" HorizontalAlignment="Right" Padding="12,4" Click="OnSettingsClick"/>
        </DockPanel>
    </Grid>
</Window>
